# UI-REDESIGN-TRACK — مسار إعادة تصميم الواجهة (Design Closure 2026)

> هذا المستند يعرّف المسار، يسجّل كل شريحة مُسلَّمة، ويحدّد الشريحة
> التالية. المرجع الحاكم: ADR-6 (03-adr-set.md §10) + خطة §12/§13
> (04-implementation-plan.md).

## قرار المسار (حاكم)

إعادة التصميم تجري **متزامنة مع تفكيك ADR-6** (خيار ج): لا إعادة تصميم
فوق MainViewModel دون تفكيك (تضخيم الدين)، ولا تفكيك كامل مسبق (تسلسل
بطيء). لكل شريحة: ViewModel ميزة مستقل + سلوكه المختبر (GAP-21) +
إعادة التصميم البصري للشاشات التي تملكها.

## الشريحة 1 (مُسلَّمة — Phase 5)

### ما سُلِّم

1. **FilesViewModel** (استخراج كامل — أول نمط مكتمل لـ ADR-6):
   - الحالة: `files / selectedFilePath / selectedFileContent / isFileLoading`
     غادرت `UiState` المشتركة (264→~255 حقلاً) إلى `FilesUiState` خاصة؛
     MainViewModel فقد 6 دوال + `currentProjectIdOrInform` + تبعية
     `manageWorkspaceFilesUseCase` بالكامل.
   - السلوك: تحديث تلقائي للقائمة عند تغيّر المشروع النشط (يحل محل
     استدعاء `refreshFiles()` في `observeWorkspace` القديم) مع إزالة
     تكرار بمعرّف المشروع.
   - الاختبارات: `FilesViewModelTest` — 15 اختبار سلوكي (Outcome صادق،
     بوابة المشروع بوعي حالة الإقلاع، دورة المحرر، الحفظ/الحذف،
     التبديل التلقائي).

2. **SettingsViewModel** (إدارة مساحات العمل):
   - `switchWorkspace / createWorkspace / updateWorkspaceNetworkPolicy /
     setAutonomyPolicy` — كلها عبر WorkspaceRuntimeService السلطوي. `setAutonomyPolicy` حُذفت من
     MainViewModel نهائياً؛ مرآة العرض `UiState.autonomyPolicy` تُزامَن
     الآن من العمود المُخزَّن في `observeWorkspace`.
   - تحسين صدق: خدمة التبديل تُرجع false لمساحة غير موجودة (بلا رمي) —
     كان ذلك صمتاً في UI؛ الآن يظهر رسالة صريحة.
   - الاختبارات: `SettingsViewModelTest` — 7 اختبارات (الدوال الجديدة +
     عمود الاستقلالية السلطوي + فشل التبديل الصادق).

3. **إعادة تصميم الشاشات الثلاث**:
   - Settings: **إصلاح صدق** — بطاقة «حول» كانت تعرض «Room v12» بينما
     القاعدة v17؛ الآن تقرأ `AppDatabase.SCHEMA_VERSION` (مصدر الحقيقة
     الواحد). صفوف السياسات تحمل شرحاً بلغة واضحة، بطاقات مساحات
     بهيكل معلومات + شارات.
   - Files: ترتيب (الاسم/الحجم/آخر تعديل)، مجلدات أولاً في مجموعة
     مسماة، عنوان حي بعدادات (مجلد/ملف/حجم)، بانر تشخيصي محلي.
   - Explorer: أربع أقسام مسماة (ذكاء التنفيذ / الموارد والأدوات /
     المعرفة والمحتى / الخطط والجلسات)، شارة عدد + سهم لكل صف، وعدد
     الملفات من FilesViewModel.
   - Studio: شريط السياسات يستقبل `onAutonomyPolicy` من
     SettingsViewModel (العرض في MainViewModel، التغيير في ميزة الإعدادات).

4. **CI — إصلاح البناء التلقائي للـ apk-debug** (طلب المستخدم الصريح):
   - **الجذر**: android.yml كان فاشلاً في **52/52 تشغيلة** منذ إنشائه —
     قيمة `name:` في الخطوة 43 تحوي `": "` غير مُقتبسة → ملف YAML غير
     صالح → لا تُنشأ أي jobs أصلاً. build-apk.yml (المحذوف في Phase 3)
     كان المنتج الفعلي الوحيد للـ APK، فبقي المستودع بلا بناء APK على
     GitHub بعد دمج Phase 3.
   - **إصلاح ثانٍ ( signing)**: خطوة bundleRelease كانت تحقن قيم
     توقيع فارغة (`-Pandroid.injected.signing.*=`) — يبني AGP إعداد
     توقيع `externalOverride` مكسوراً ويفشل `validateSigningRelease`.
     الآن: `build.gradle.kts` لا يُسند signingConfig للـ release إلا
     عند توفر KEYSTORE_PATH أو ملف مفتاح محلي → AAB غير موقّع صادق في
     CI، والموقّع في release.yml (الذي يزوّد KEYSTORE_PATH).
   - **التحقق المحلي (CI-تساوي)**: compileDebugKotlin ✓،
     testDebugUnitTest ✓، assembleDebug ✓، bundleRelease ✓ (AAB غير
     موقّع 36MB) — أول تشغيل ناجح مُوثّق لسلسلة android.yml كاملة.
   - اسم artifact الـ APK: `ai-v0-ultimate-debug-apk` (نفس اصطلاح
     build-apk.yml المحذوف) + retention 14 يوماً.

### حالة e2e-device.yml (الجذر الحقيقي مُشخَّص ومُصلَح — Phase 8)

**التشغيلة 16 (على دمج Phase 7 نفسه): فاشلة** — بأدلة عامة موثّقة: أيقونة
حمراء على `965fcd4`، تعليق الخطوة نفسه ("The process '/usr/bin/sh' failed
with exit code 1" عند `#step:7:516`)، مدة 4m36s (مطابقة تقريباً لـ4m35s
التشغيلات 1–15: بناء ناجح + محاكي أقلع + اختبارات عملت)، وartifact
النتائج 20.4KB (مقابل 19.6KB — ملف نتائج للاختبارات التي ركضت).

**الجذر (مُثبت بإعادة إنتاج محلية كاملة)**: توكيد **أضافته Phase 7 نفسها**
إلى اختبار الإقلاع كان هو الفشل —
`assertEquals(SCHEMA_VERSION.toLong(), readableDatabase.version)`:
توقيع `SupportSQLiteDatabase.getVersion()` في androidx.sqlite 2.5.0 هو
**`int`**، فاختار Kotlin التحميلة `assertEquals(Object, Object)` وأصبح
`Long(17) ≠ Integer(17)` — **فشل حتمي في كل بيئة** (المحاكي الفعلي
منها). المرآة الجديدة
`E2ESuiteRobolectricMirrorTest` أعادت إنتاج الفشل محلياً حرفياً قبل
الإصلاح (`expected: java.lang.Long<17> but was: java.lang.Integer<17>`)
— علماً أن جسم اختبار موت العملية (اختبار 2) عبر بنجاح في المرآة:
إصلاح السِنغلتِن في Phase 7 كان صحيحاً وعاملاً؛ ما أبقى التشغيلة حمراء
هو التوكيد الجديد فقط.

**الإصلاح**: توسيع **الجانبين** إلى `long`
(`SCHEMA_VERSION.toLong()` و `version.toLong()`) — تبقى التحميلة
البدائية `(long, long)` ولا صندقة كائنات. الإصلاح مطبّق في الاختبار
وفي مرآته معاً.

**درس منهجي (لماذا لم تلتقطه مجموعات JVM قبل الدمج)**: مرآة Phase 7
(`AppDatabaseSingletonProcessDeathTest`) غطّت تسلسل اختبار 2 وحده؛
توكيد اختبار 1 لم يُنفَّذ قط على JVM قبل الدمج. المرآة الجديدة تغطي
**المجموعة كاملة** (الأجسام الأربعة حرفياً، بثلاثة ترتيبات، عملية
واحدة وملف DB واحد مشترك كما على المحاكي) — وكانت هي الكاشف الفعلي
هنا، وهي تبقى شبكة الانحدار الدائمة للمسار.

**التحقق المحلي (تشغيلات فعلية، JUnit XML)**: المرآة 3/3 خضراء بعد
الإصلاح (كانت 3/3 حمراء قبله — RED→GREEN موثّق)؛ كامل
`testDebugUnitTest`: **111 suites / 711 tests / 0 failures / 0 errors /
0 skipped** (العدد ارتفع من 110/708 بمرآة المجموعة)؛
`compileDebugAndroidTestKotlin` و `assembleDebugAndroidTest`
(960,856 bytes) و `assembleDebug` (100,270,222 bytes) ناجحة كلها.

**حكم الـ CI الفعلي**: معلق على التشغيلة 17 بعد دمج هذا الـ patch —
لا يُدَّعى النجاح قبل حدوثه. (apk-debug مستقل عن هذا المسار كما كان.)

## الشريحة 2 (مُسلَّمة — Phase 6)

### ما سُلِّم

1. **StudioViewModel** (زمن التشغيل المحادثي — الأثقل كما وُصف في الجدول):
   - الحالة: 18 حقلاً غادرت `UiState` (promptInput / isExecuting /
     executionLog / streamText / studioSession / sessionTurnStartMs /
     chatMode / activeSessionId / selectedModelResourceId /
     selectedModelDisplayName / isDegraded / degradedReason /
     currentTokensConsumed / sessionTotalTokens / remainingBudget /
     networkPolicy / قناتا الخطأ والبانر الخاصتين بالميزة) إلى
     `StudioUiState` خاصة.
   - السلوك: نواة التنفيذ كاملة (`executePrompt` / `cancelExecution` عبر
     ExecutionHost المُسنَد إلى مساحة العمل)، quick-chat عبر الوكيل
     القانوني، تثبيت النموذج (`selectModel` → `setSessionModel`)،
     `setNetworkPolicy` (مدخل الجلسة)، ضمان الجلسة الدائمة قبل التنفيذ
     (`ensureActiveSession`)، وحفظ الدورات (`persistTurnDurably` مع
     التسمية من أول سؤال).
   - **درزة الوكيل**: `activeAgent` بقيت حالة مشتركة في MainViewModel
     (يقرؤها TasksScreen وExplorerScreen) — الشاشة تمرر الوكيل المختار
     معاملاً إلى `executePrompt(agent)` / `startNewSession(agent)`.
   - **ناقل إشارات الميزة** (`StudioSignal`): أحداث التنفيذ ذات الأثر
     العابر للميزة (Started للربط بتتبع النشاط، DecisionMade /
     ObservationRecorded / Completed / Error لمرايا العرض القرارية) +
     تغيّر سياسة الشبكة تُنشَر على `MutableSharedFlow` يملكه MainActivity؛
     MainViewModel يجمعها في `observeStudioSignals` ويحدّث المرايا —
     لا حالة قابلة للتغيير مشتركة بين الاثنين.

2. **SessionsViewModel** (سجل الجلسات الدائمة):
   - الحالة: قائمة الجلسات (نطاق GAP-14: مشروع/مشترك)، علم متصفح
     الجلسات، قناة خطأ خاصة.
   - السلوك: المراقبة عبر **flatMapLatest** — إصلاح سباق «آخر كاتب»
     الموروث (المراقب القديم كان يُطلق جامعاً جديداً لكل تبديل دون
     إلغاء السابق، فتتراكم الجامعات ويتنافس إرسالها). `deleteSession`
     تُخطِر الاستوديو عبر callback يُطلق بعد استقرار الاستدعاء (ترتيب
     حتمي لمسح الربط النشط).

3. **تكليف MainViewModel** (تقلّص من 2293 إلى ~1730 سطراً):
   - فُقدت تبعيتان كاملتان: `conversationSessionService` و`appContext`
     (الأخيرة كانت لأجل خدمة التنفيذ الأمامية فقط).
   - مرايا العرض القرارية (latestDecision / decisionUncertainty /
     caseBaseList) وشبكة الجلسة (networkPolicy) تبقى لكن مصدرها ناقل
     الإشارات — نفس نمط autonomyPolicy من الشريحة 1.

4. **الشاشات**:
   - Studio: تركيب ثلاثي (viewModel للكتالوج/الموارد، studioViewModel
     للمحادثة، sessionsViewModel للسجل)؛ شريط الجلسات يحسب عنوان الجلسة
     النشطة من قائمة السجل بمعرّف ربط المحادثة.
   - Settings: قسم «سياسات التنفيذ (الجلسة)» يعرض ويغيّر سياسة الشبكة
     عبر قيمة + lambda إلى ميزة الاستوديو (نمط تفويض الشريحة 1 معكوساً).
   - Explorer: صف «الجلسات» يقرأ القائمة من SessionsViewModel (صاحبها).

5. **GAP-21 — الاختبارات السلوكية**: `StudioViewModelTest` (18 اختباراً
   عبر النواة الحقيقية: ExecuteAgentTaskUseCase → AgentOrchestrator مع
   مزوّد LLM محلي وهمي عند PORT فقط، وWorkspaceRuntimeService و
   ConversationSessionService الحقيقيين فوق مزيفات DAO/المستودع) +
   `SessionsViewModelTest` (8 اختبارات: عزل نطاق GAP-14، إعادة النطاق
   بالتبديل، حذف بترتيب callback). 26 اختباراً جديداً.

### صدق مكتشف (سلوك قائم لم يُغيَّر — موثَّق)

- طوبولوجيا أحداث النهاية عند فشل المزود: خطأ المزود يمرّ عبر
  ExecutionService **ثم** تصدر الحلقة `Completed` بنص الاحتياط
  «اكتملت معالجة المهمة.» — فيُلحق دوران (فاشل ثم «ناجح») في النص.
  هذا سلوك خط الأنابيب قبل الشريحة نفسه (المجمع المنقول حرفياً)،
  أثبته الاختبار كما هو؛ أي تصحيح له عقد التنفيذ الطرفي هو قرار
  مستقبلي (شريحة Decision) وليس من نطاق إعادة التصميم.
- إلغاء الاستوديو كان يصفّر `isExecutingWorkflow` ( علم ميزة المهام)
  دون أن يُلغي فعلاً تنفيذ خطة العمل الجارية — كذبة عرض موروثة؛ بعد
  التفكيك كل ميزة تصفّر علمها فقط.
- المراقب القديم لقائمة الجلسات كان يراكم الجامعات (سباق آخر كاتب) —
  أُصلح في SessionsViewModel بـ flatMapLatest (الملاحظة نفسها نقلت،
  فكُتبت صحيحة).

### التحقق من CI (طلب المستخدم الصريح — مُثبَت بقرار حقيقي)

- **android.yml ناجح على GitHub**: التشغيلة #53 «Build Android APK»
  على `0966d3d` (دمج المرحلة 5) — `completed / success` بتاريخ
  2026-09-13T15:24:42Z. نجاح الـ job يشمل خطوات التحميل (لا ينجح
  إلا إذا خرجت كل خطوة بـ 0)، فتتأكد آلية `ai-v0-ultimate-debug-apk`.
  أول نجاح كامل للـ workflow بعد 52 فشلاً متتالياً (الخلل YAML).
- e2e-device.yml: الجذر الأول (سِنغلتِن `AppDatabase` يعيد المثيل المغلق
  بعد `close()` في اختبار إعادة الفتح) شُخِّص وأُصلِح في Phase 7؛ كشف
  التشغيلة 16 جذراً ثانياً في التوكيد الذي أضافته Phase 7 نفسها —
  شُخِّص وأُصلِح في Phase 8 (الجذران والتحقق في القسم المخصّص أعلاه)؛
  الحكم النهائي = التشغيلة 17 بعد الدمج.

## الشريحة 3 (مُسلَّمة — Phase 8)

### ما سُلِّم

1. **KnowledgeViewModel** (قاعدة المعرفة + الذاكرة + المحرك الدلالي):
   - الحالة: 11 حقلاً غادرت `UiState` المشتركة (knowledgeDocuments /
     assembledRagContext / newDocTitle / newDocContent / **semanticModelReady /
     isProvisioningSemanticModel** — المشتركة سابقاً — / memoryQuery /
     retrievedMemories / allMemories / isSearchingMemory / newMemoryContent)
     إلى `KnowledgeUiState` خاصة، مع قناتَي خطأ وبانر خاصتين بالميزة.
   - السلوك (منقولاً حرفياً): الإدخال بتطهير العنوان (P0-8) مع صدق فشل
     الحفظ (P1-14)، الاسترجاع الهجين، الحذف بنتيجته الصادقة، تجهيز
     النموذج الدلالي المحلي (GAP-07)، مرآة الجاهزية (GAP-25)، وعمليات
     الذاكرة كاملة (بحث/إضافة/تحديث) عبر `ManageMemoryUseCase`.
   - **إعادة التكييف**: جامع المستندات الحية انتقل من `observeSubsystems`
     (كان آخر مسؤولية معرفية فيها)، وإعادة تحميل الفهرس عند تبديل مساحة
     العمل انتقلت من `observeWorkspace`، والتحميل الأولي للذكريات انتقل
     من `loadInitialData` — MainViewModel فقدت تبعيتَي
     `ragPipelineService` و`manageMemoryUseCase` بالكامل (1730→1630
     سطراً).

2. **الشاشات** (تركيب على الحالة المفككة):
   - Knowledge: تبويبات بعناوين حية بعدادات (مستندات/ذكريات)، عنوان
     قسم الاسترجاع يقرأ جاهزية المحرك من مالكها، بانر تشخيصي محلي
     قابل للإخفاء (نمط الشريحتين 1–2).
   - Settings: بطاقة المحرك الدلالي تستقبل **قيمة + lambda** من
     ميزة المعرفة (نمط تفويض سياسة الجلسة من الشريحة 2 معكوساً).
   - Explorer: صف «المعرفة» يقرأ العدد والعنوان الفرعي من
     `knowledgeViewModel` (نمط صفوف الملفات/الجلسات).
   - MainAppScreen: قناة خطأ المعرفة تُعرض في snackbar العام من
     مصدر حقيقتها الخاص وتُخفى من مالكها (نمط الاستوديو).

3. **GAP-21 — الاختبارات السلوكية**: `KnowledgeViewModelTest` —
   **21 اختباراً** فوق الخدمات الحقيقية: `RagPipelineService` الحقيقي
   (بلا تزييف عند مستوى الخدمة؛ مزيفات DAO/المنفذ فقط) و
   `ManageMemoryUseCase` الحقيقي فوق مستودع ذاكرة في الذاكرة. أبرز ما
   يُثبَّت: إعادة تحميل المعرفة بتغيّر مساحة العمل النشطة؛ **مسار نجاح
   التجهيز عبر الموجّه الحقيقي** (`LocalSemanticEmbeddingRouter` فوق
   `OnnxSemanticEmbeddingAdapter` حقيقي بملفات نموذج مزيفة تحت
   Robolectric — `isProvisioned` بفحص المهايئ نفسه، وprovision ينجح
   بلا شبكة) ومسار فشله الصادق؛ تطهير العنوان؛ تشخيص FAILED الحقيقي؛
   متغيرات Outcome الثلاثة للذاكرة (مع بقاء نص المستخدم عند فشل
   التخزين). مساعد `awaitUntil` موثّق للأعمال التي تقفز إلى
   Dispatchers حقيقية داخل الخدمة.

### التحقق (تشغيلات فعلية)

- كامل `testDebugUnitTest` بـ `--rerun-tasks`: **112 suites / 732
  tests / 0 failures / 0 errors / 0 skipped** (ارتفع من 111/711
  بالشريحة والمرآة).
- `assembleDebug` (100,623,032 bytes) و`compileDebugAndroidTestKotlin`
  ناجحة.

## الشريحة 4 (مُسلَّمة — Phase 9)

### ما سُلِّم

1. **GovernanceViewModel** (مرصد الحوكمة + سطح الموافقات البشرية):
   - الحالة: 10 حقول غادرت `UiState` المشتركة (radarCapabilityStatuses /
     radarRecommendations / radarChanges / workspaceBudgetStatus /
     costLedgerRecent / workspaceTokensConsumed / budgetAllocationInputUsd /
     isSavingBudgetAllocation / pendingApprovals / measurementHealth) إلى
     `GovernanceUiState` خاصة، مع قناتَي خطأ وبانر خاصتين بالميزة.
   - السلوك (منقولاً حرفياً): مراقبو الرادار المدعومون بـ Room، التحديث
     عند الطلب (لقطة الرادار + ملخص الميزانية/السجل + صحة القياس GAP-24 +
     طابور الموافقات)، محرر تخصيص الميزانية (عبر ManageWorkspaceBudgetUseCase
     الحقيقي)، إخفاء التوصيات، و**حلقة الموافقة كاملة** (approve / reject /
     «السماح دائماً» بمنح EXECUTE الدائم ADR-2c) — كلها بعزو المُدير
     المحلي الصريح (GAP-02).
   - **إعادة التكييف (الإصلاح الوحيد المتعمَّد، بسابقة الشريحة 2)**:
     مراقبو الرادار كانوا بنمط «الجامعات المتراكمة» (جامع داخلي جديد لكل
     انبعاث لمساحة العمل دون إلغاء السابق — نفس عائلة سباق «آخر كاتب»
     الذي أصلحته الشريحة 2 في سجل الجلسات). أُعيدت كتابتهم بـ
     **flatMapLatest**: المرصد يعكس مساحة العمل النشطة فقط، ولا يمكن
     لكتابة من مساحة قديمة أن تنزف إليه.
   - **مرآة سياسة الجلسة**: تبقى مدخل لقطة الرادار؛ GovernanceViewModel
     يحتفظ بمرآته الخاصة متزامنة من ناقل إشارات الاستوديو (نفس الدرزة
     الصريحة لمرايا MainViewModel القرارية — لا حالة مشتركة قابلة
     للتغيير بين الاثنين، كل ميزة تجمع حصتها بنفسها).
   - MainViewModel فقد **سبع تبعيات كاملة** (capabilityRadarService،
     economicGovernanceService، humanApprovalGate، permissionGrantService،
     manageWorkspaceBudgetUseCase، networkMonitorProvider، localPrincipalId)
     وتقلّص من 1630 إلى 1385 سطراً.

2. **الشاشات** (تركيب على الحالة المفككة):
   - Governance: تقرأ من مالكها (GovernanceViewModel.state)، مع **بانر
     تشخيصي محلي قابل للإخفاء** (نمط المعرفة من الشريحة 3) لنتائج
     الموافقات وتدهورات التحديث.
   - MainAppScreen: قناة خطأ الحوكمة تُعرض في snackbar العام من مصدر
     حقيقتها الخاص (نمط الاستوديو/المعرفة).

3. **GAP-21 — الاختبارات السلوكية**: `GovernanceViewModelTest` —
   **13 اختباراً** فوق الخدمات الحقيقية: بوابة الموافقة الحقيقية فوق
   `RoomHumanApprovalStore` (Room في الذاكرة تحت Robolectric)، خدمة المنح
   الحقيقية فوق `permissionGrantDao` الحقيقي (المنح الدائم يُستعاد
   بالاستعلام عبر الخدمة نفسها)، الرادار الحقيقي فوق مزيف المنفذ،
   والاقتصاد الحقيقي + حالة الاستخدام الحقيقية للميزانية. أبرز ما
   يُثبَّت: عزو الموافقة/الرفض بهوية المُدير من المُنشئ؛ المنح الدائم
   يحلّ الطلب الجاري ويُسجَّل فعلاً؛ الخطأ الصادق عند غياب خدمة المنح؛
   حفظ الميزانية عبر الاستخدام الحقيقي مع صدى التنسيق؛ البوابة الصادقة
   عند غياب مساحة عمل جاهزة (P0-03 — بمؤقّت افتراضي على مجدوِل
   الاختبار، بلا انتظار حقيقي)؛ مرآة السياسة من الناقل؛ **إعادة النطاق
   بـ flatMapLatest** (كتابة من مساحة قديمة لا تنزف إلى المرصد)؛ إخفاء
   التوصية عبر الخدمة الحقيقية.

### صدق مكتشف (إصلاح بنية اختبار — لا سلوك منتج)

- `FakeRadarPersistence.observeRecommendations` كان يُبقي التوصيات
  **المخفاة** في التدفق المراقَب، بينما استعلام Room الحقيقي
  (GovernanceDaos) يرشّح `isDismissed = 0` — المزيف كان ينحرف عن دلالات
  المنفذ الحقيقي. حُسِّن ليوافقها (لا مستهلك آخر كان يعتمد على السلوك
  المنحرف). كشفه اختبار الإخفاء الجديد.

### التحقق (تشغيلات فعلية)

- `testDebugUnitTest` الكاملة: **113 suites / 745 tests / 0 failures /
  0 errors / 0 skipped** (ارتفع من 112/732 بـ 13 اختبار الحوكمة).
- `compileDebugKotlin` و`compileDebugUnitTestKotlin` و
  `compileDebugAndroidTestKotlin` ناجحة.
- `assembleDebug` ناجحة (100,270,222 bytes — وتحقق إضافي أن صنف
  GovernanceViewModel داخل dex الحزمة).

## الشريحة 5 (مُسلَّمة — Phase 10)

### ما سُلِّم

1. **ProvidersViewModel** (غرفة تحكم المزوّدين والموارد — أكبر شاشة
   متبقية، 1231 سطراً):
   - الحالة: 19 حقلاً غادرت `UiState` المشتركة (generalizedProviders /
     generalizedServices / generalizedConfigurations / discoveredOfferings /
     materializedResources / isDiscoveringModels / isTestingProvider /
     testingProviderId + حقول معالج الربط الستة + حقول حوار المفتاح
     الخمسة) إلى `ProvidersUiState` خاصة، مع قناتَي خطأ وبانر خاصتين
     بالميزة.
   - السلوك (منقولاً حرفياً): جامعات مستوى التحكم الأربعة (Room)،
     بذرة الإقلاع الأولى للمزوّدين (`launchBootstrapDefaults` — كانت في
     init الخاص بـ MainViewModel؛ نفس نقطة الاستدعاء: إنشاء VM الميزة
     عند النشاط)، فحص اتصال الخدمة، اكتشاف العروض، تجسيد/تحقق/تفعيل/
     تعطيل الموارد، حذف/تبديل المزوّد، **معالج الربط الكامل** (السلسلة
     الموجّهة ذات الخطوات السبع عبر ConnectProviderUseCase الحقيقي)،
     وحوار إدخال المفتاح (F-4: حفظ في القبو المشفّر + إعادة فحص فورية
     + ترقية موارد الخدمة).
   - MainViewModel فقد **تبعيتين كاملتين** (providerControlPlaneService
     وconnectProviderUseCase) وأربع عشرة دالة وأربعة جامعات واستدعاء
     البذرة (1384→1052 سطراً).

2. **الشاشات** (تركيب على الحالة المفككة):
   - Providers: تُركَّب على مالكها (viewModel.state) — بلا أي مرور
     للحالة المشتركة.
   - MainAppScreen: شارات الرأس (ذكاء نشط/موارد مفعّلة) تقرأ تدفق
     الميزة؛ قناة خطأ المزوّدين في snackbar العام وبانرها التشخيصي في
     السطح العام — كلاهما من مصدر حقيقتها الخاص.
   - Dashboard: بطاقات الإحصاء الثلاث تقرأ من مالك الميزة.
   - Studio: بوابة «اربط مزوداً» ومنتقي النماذج يقرآن موارد الميزة
     (نمط تركيب الميزات في الشريحة 2).
   - Explorer: صفّا النماذج والموارد يقرآن العدد والعنوان الفرعي من
     مالك الميزة (نمط صفوف الملفات/الجلسات/المعرفة).

3. **GAP-21 — الاختبارات السلوكية**: `ProvidersViewModelTest` —
   **20 اختباراً** فوق الخدمات الحقيقية: مستوى التحكم الحقيقي فوق
   مستودعات Room حقيقية (ذاكرة داخلية تحت Robolectric) مع مصنع
   المهايئات الحقيقي وسجل المصادقين الحقيقي؛ **EgressControl خاص
   غير مُثبَّت** (لا سياسة مساحة → كل طلب صادر يُرفض fail-closed قبل
   أي مقبس) — فشائل تحقق/اكتشاف **حتمية بلا شبكة** وبلا تلوث عابر
   للحزم؛ **ConnectProviderUseCase الحقيقي** — اختبار المعالج يمشي
   السلسلة كاملة وينتهي SavedUnverified بصدق عند رفض الخروج؛ القبو
   **الحقيقي** لِعَقد S-2 (الرفض الصريح بلا بديل برمجي غير آمن) ومزيف
   المنفذ لمسار النجاح. أبرز ما يُثبَّت: البذرة تُنشئ local/multi_source/
   google مع مورد Gemini **مسجَّل بصدق** (REGISTERED — لا ENABLED
   مفبرك بلا مفتاح)؛ المعالج يرفض بلا مفتاح قبل أي كتابة؛ السلسلة
   الكاملة تُخلِّد المزوّد→الخدمة→الإعداد→العرض→المورد والمفتاح
   قابل للاسترجاع من القبو؛ فشل القبو الصادق يوقف السلسلة عند الخطوة
   3 ولا يُخلِّد أي مورد؛ إدخال المفتاح يُقلِّم النص ويخزّنه ويغلق
   الحوار ويعيد الفحص **بالمفتاح المخزَّن** (رسالة «لا مفتاح» تختفي
   ويرفض egress الطلب الفعلي)؛ NOT_VALIDATED الصادحة لل مورد غير
   المُتحقق؛ الحذف المتسلسل؛ القناتان خاصتان بالميزة وقابلتان للإخفاء.

### صدق مُكتشَف (الإصلاح المتعمَّد الوحيد — بسابقة flatMapLatest في الشريحة 4)

- **عيب صدق العرض في مسار S-2**: `EncryptedSecretStorageAdapter` كان
  يضع رسائله العربية (بما فيها رسالة رفض S-2 الصريحة: «فشل الحفظ:
  مخزن مفاتيح الجهاز غير متاح…») في خانة `failure` وحيدة المعامل،
  بينما الواجهة تقرأ `diagnosticMessage` — **فكان snackbar الخطأ يظهر
  فارغاً**: «الفشل الصريح الذي يراه المستخدم» (مقصد S-2) لم يكن يصل
  أصلاً. الإصلاح: الرسالة تنتقل إلى `diagnosticMessage` وخانة
  `failure` تحمل رمزاً آلياً (SECURE_STORAGE_UNAVAILABLE /
  SECRET_ENCRYPTION_FAILED / SECRET_DECRYPTION_FAILED /
  SECRET_DELETE_FAILED) — في المواضع الخمسة. كاشفه اختبار «فشل القبو
  الصادق» الجديد (كان التوكيد الأول يفشل برسالة فارغة تماماً).
  اختبار S-2 القائم (P0BridgeRobolectricTest) يفحص `is Error` فقط
  فلم يتأثر.

### التحقق (تشغيلات فعلية، JUnit XML)

- المجموعة الموجهة `ProvidersViewModelTest`: **20/20 خضراء** (بعد
  إصلاح سباقات جانب الاختبار: الانتظار على المجموعة الكاملة المبذورة
  لا على «غير الفارغ»، والانتظار على التشخيص النهائي EGRESS_BLOCKED
  لا على نفي رسالة عابرة — موثقة، لا تغيير منتج).
- كامل `testDebugUnitTest`: **114 suites / 765 tests / 0 failures /
  0 errors / 0 skipped** (ارتفع من 113/745 بـ 20 اختبار المزوّدين).
- `compileDebugKotlin` و`compileDebugUnitTestKotlin` و
  `compileDebugAndroidTestKotlin` ناجحة.
- `assembleDebug` ناجحة (100,270,222 بايت — وتحقق إضافي أن صنف
  ProvidersViewModel داخل dex الحزمة).

## الشريحة 6 (مُسلَّمة — Phase 11)

### ما سُلِّم

1. **RadarViewModel** (مرصد التطور التقني):
   - الحالة: 3 حقول غادرت `UiState` المشتركة (radarItems /
     evolutionCandidates / isRadarRefreshing) إلى `RadarUiState` خاصة،
     مع قناتَي خطأ وبانر خاصتين بالميزة.
   - السلوك (منقولاً حرفياً): جامعا الأنابيب (كانا آخر مسؤولية رادارية
     في `observeSubsystems`)، التحديث بكاتب السبينر الصادق (GAP-23)،
     ودورة حياة الترقية كاملة (F-10: عرض حكم بوابة الحوكمة بصدق،
     P1-17: التدقيق الأمني + الموافقة + القياس + التقاعد).
   - الشاشة: Radar تُركَّب على مالكها مع **بانر تشخيصي محلي قابل
     للإخفاء** (نمط المعرفة/الحوكمة من الشريحتين 3–4) — نتائج الترقية
     والتدقيق تُعرض سياقياً بجوار المرشحات التي تخصها.

2. **DecisionViewModel** (قمرة القيادة CBR-MDP):
   - الحالة: 5 حقول غادرت `UiState` (latestDecision / caseBaseList /
     isSimulatingDecision / decisionTaskComplexity / decisionUncertainty)
     **+ مرآة سياسة الجلسة** (كانت الحقل الوحيد المتبقي من عائلة
     المرايا المشتركة) إلى `DecisionUiState` خاصة.
   - السلوك (منقولاً حرفياً): المنزلقات بإعادة محاكاة حية، المحاكاة
     عبر `DecisionSimulationUseCase` الحقيقي (GAP-19) مع مسار الاحتياط
     الصادق عند غيابه، **واستدعاء المحاكاة الأولية انتقل من
     `loadInitialData` إلى init الميزة** (نفس نقطة الاستدعاء).
   - **درزة ناقل الإشارات (أهم درزة في الشريحة)**: نصيب القرار من
     ناقل StudioSignal أصبح يُجمع هنا (نمط GovernanceViewModel — كل
     ميزة تجمع حصتها بنفسها): DecisionMade / ObservationRecorded /
     Completed / Error للمرايا، وNetworkPolicyChanged للمرآة + إعادة
     المحاكاة. MainViewModel أبقى **Started فقط** (ربط تتبع النشاط).
   - الشاشة: Decision تُركَّب على مالكها.

3. **WorkflowsViewModel** (منشئ خطط العمل + المكتبة الدائمة + الاستئناف
   — «Tasks المتبقي»):
   - الحالة: 5 حقول غادرت `UiState` (workflowReport /
     isExecutingWorkflow / workflowBuilder / workflowLibrary /
     resumableWorkflows) إلى `WorkflowsUiState` خاصة. أنواع المنشئ
     (WorkflowBuilderStep/State) بقيت في حزمة الحالة (سابقة StudioTurn
     من الشريحة 2).
   - السلوك (منقولاً حرفياً): التنفيذ ببذر البذور المكتملة (الاستئناف
     الدائم)، المنشئ الكامل (إضافة/تحرير/ترتيب/تبعيات/الربط القانوني
     للوكيل)، دورة حياة المكتبة (حفظ→سرد→تحميل→تحرير→إعادة حفظ/
     استنساخ/حذف/تشغيل بتاريخ)، قائمة الاستئناف النطاقية، والاستئناف
     نفسه — لوحة المهام الدائمة بقيت في TasksViewModel (مالكها منذ
     GAP-11).
   - **إعادة التكييف (الإصلاح المتعمَّد الوحيد، بسابقة الشريحة 4)**:
     مراقب الأصول المكتبية كان بنمط «الجامعات المتراكمة» (جامع مكتبة
     داخلي جديد لكل انبعاث مساحة عمل دون إلغاء السابق). أُعيدت كتابته
     بـ **flatMapLatest**: المكتبة تعكس مساحة العمل النشطة فقط،
     ومساحة الـnull هي القائمة الفارغة الصادقة (سابقة عزل GAP-04)
     لا صفوف مساحة قديمة راكدة.
   - الشاشات: Tasks تركيب ثلاثي (MainViewModel للكتالوج فقط — درزة
     الاستوديو نفسها، + WorkflowsViewModel للميزة + TasksViewModel
     للوحة)؛ صف «خطط العمل» في المستكشف يقرأ من مالك الميزة (نمط
     صفوف الملفات/الجلسات/المعرفة/المزوّدين).

4. **تقلّص MainViewModel**: 1052 → **628 سطراً**. فُقدت ست تبعيات
   كاملة (cbrMdpEngine، decisionSimulationUseCase،
   intelligenceRadarPipeline، executeWorkflowUseCase،
   workflowLibraryService، workflowPersistenceService) وثلاثة
   وعشرون دالة وخمسة جامعات ومرآة الشبكة المشتركة. المتبقي: الإقلاع،
   الوكلاء، الامتدادات، تدفق النشاط، ومرايا مساحة العمل — كلها
   موثقة للشرائح اللاحقة.

5. **مصفوفة Roborazzi (GAP-21 — مطلب الشريحة)**:
   `Slice6ScreensRoborazziMatrixTest` — 4 لقطات Pixel8 بـ **RTL مثبَّت
   كالإنتاج** (LocalLayoutDirection)، فوق خدمات حقيقية بلا شبكة:
   radar_loaded، decision_empty (الاحتياط الصادق بلا حالة استخدام —
   لا VERDICT مفبرك) + decision_loaded (تقييم المحرك الحقيقي)،
   tasks_builder_console (قالب المنشئ + تعريف مكتبة حقيقي + تنفيذ
   RUNNING دائم قابل للاستئناف). تُسجَّل بـ `recordRoborazziDebug` في
   `app/src/test/screenshots/slice6/`.

### إصلاحا صدق متعمَّدان (سابقة S-2 من الشريحة 5)

- **عيب صدق العرض في «المرشح غير موجود»**: المواضع الخمسة ذات المعامل
  الواحد في `IntelligenceRadarPipeline` كانت تضع الرسالة العربية في
  خانة `failure` بينما الواجهة (الكود المنقول حرفياً من MainViewModel
  — العيب كان موجوداً قبل الشريحة) تقرأ `diagnosticMessage` — فكان
  snackbar الخطأ **فارغاً** عند ترقية/تدقيق مرشح غير موجود. الإصلاح:
  الرسالة تنتقل إلى `diagnosticMessage` و`failure` يحمل رمزاً آلياً
  `CANDIDATE_NOT_FOUND` — نفس عائلة S-2 بالضبط. كاشفه اختبار «مرشح
  مجهول» الجديد (التوكيد الأول فشل برسالة فارغة تماماً).
- **كاتب حقيقي لقناة خطأ القرار**: استدعاء حالة الاستخدام في
  `simulateDecisionInternal` أصبح محاطاً بـ runCatching يوجّه الفشل
  إلى `errorMessage` (قناة الميزة التي يعرضها snackbar العام) — فشل
  محرك غير مقبول كان يسقط coroutine كاملة بلا سطح مرئي للمستخدم.

### حسم القرار المؤجَّل: طوبولوجيا الحدث الطرفي (D-11)

الشريحة 2 وثّقت أن «أي تصحيح لعقد التنفيذ الطرفي هو قرار مستقبلي (شريحة
Decision)». حُسم الآن كقرار منتج موثَّت **D-11** في
`docs/PRODUCT-DECISIONS.md`: الطوبولوجيا تبقى كما هي — الحدثان
يصفان حقيقتين مختلفتين (فشل الخطوة/انتهاء الدورة، وCompleted يحمل
isDegraded أصلاً)، وتغييرها عقدُ تنفيذ ADR-4 وليس شأناً لمسار إعادة
التصميم؛ إعادة الفتح عند إعادة تصميم عرض النصّ في الاستوديو أو قرار
منتج يجعل أخطاء المزود قاتلة.

### GAP-21 — الاختبارات السلوكية

- **RadarViewModelTest** — 9 اختبارات فوق الأنابيب الحقيقية (مصادر
  صفرية = بلا شبكة) مع Room في الذاكرة: انعكاس بذرة الإقلاع تفاعلياً،
  استقرار السبينر (GAP-23)، **دورة الحوكمة الكاملة**: ترقية خطوة
  أمامية واحدة بالبانر الصادق، رفض قفز المراحل بحكم GATE_STAGE_ORDER،
  منع الدمج دون تدقيق+موافقة (GATE_GOVERNANCE_APPROVAL) ثم نجاحه بعد
  تسجيلهما عبر المسجِّلات الحقيقية، بواباتا القياس/التقاعد
  (REGISTERED فقط)، والمرشح المجهول.
- **DecisionViewModelTest** — 10 اختبارات فوق المحرك الحقيقي + ناقل
  مُحقن: المحاكاة الأولية عبر حالة الاستخدام الحقيقية، استقرار
  السبينر، الاحتياط الصادق بلا حالة استخدام (لا معاينة مفبركة)،
  إعادة المحاكاة الحية بالمنزلقين، **نصيب القرار من الناقل**: مرآة
  DecisionMade بالهوية، مرآة عدم اليقين + تحديث قاعدة الحالات
  بObservationRecorded، تحديث الحالات بCompleted، مزامنة
  NetworkPolicyChanged + إعادة المحاكاة (سلوك MainViewModel القديم
  حرفياً).
- **WorkflowsViewModelTest** — 9 اختبارات فوق **المحرك الحقيقي كاملاً**
  (ExecuteWorkflowUseCase → WorkflowEngine → AgentOrchestrator مع مزود
  LLM محلي وهمي عند PORT فقط) مع Room في الذاكرة وWorkspaceRuntime
  الحقيقي: قابلية تحرير المنشئ كاملة، حفظ→سرد→تحميل→تحرير→إعادة حفظ
  (نسب الإصدارات)، تنفيذ حقيقي بتقرير COMPLETED لكل الخطوات، الخطة
  الدورية تفشل عبر بوابة المحرك (سلطة الدورة الوحيدة — GAP-19)
  بالخطأ الصادق، **إعادة نطاق المكتبة بـ flatMapLatest** (كتابة في
  مساحة قديمة لا تنزف للقائمة النشطة؛ والعودة تُظهر صفوف المساحة
  كلها بصدق)، قائمة الاستئناف النطاقية + استئناف يُعيد التنفيذ عبر
  المحرك الحقيقي، الاستنساخ/الحذف، والقناتان القابلتان للإخفاء.
  (سباقات جانب اختبار أُصلحت وموثقة بسابقة Providers: انتظار البانر
  النهائي المعيّن لا «غير الفارغ»، وسحب قائمة الاستئناف بعد start()
  عبر بناء VM متأخر — يحاكي هبوط عملية على تنفيذ مقاطَع.)

### التحقق (تشغيلات فعلية، JUnit XML)

- المجموعات الموجهة الثلاث: **28/28 خضراء** (بعد إصلاح سباقات جانب
  الاختبار الموثقة أعلاه).
- كامل `testDebugUnitTest`: **118 suites / 797 tests / 0 failures /
  0 errors / 0 skipped** (ارتفع من 114/765 بـ 28 اختباراً سلوكياً +
  4 لقطات Roborazzi).
- ملاحظة هشاشة موثقة بصدق: تشغيلة `--rerun-tasks` الكاملة (عبء 797
  اختباراً بعاملَي عمل) أسقطت مرة واحدة `ProvidersViewModelTest::
  disableResource` بمهلة awaitUntil البالغة 5 ثوانٍ؛ إعادة تشغيلها
  المعزولة **20/20 خضراء** والتشغيل الكامل النهائي أعلاه 0 فشل —
  هشاشة حمل جانب اختبار قائمة (لا تغيير منتج)، مرشحة لرفع المهلة في
  شريحة قادمة.
- `compileDebugKotlin` و`compileDebugUnitTestKotlin` و
  `compileDebugAndroidTestKotlin` ناجحة.
- `assembleDebug` ناجحة (100,286,606 بايت) — وتحقق إضافي أن أصناف
  RadarViewModel وDecisionViewModel وWorkflowsViewModel داخل dex
  الحزمة.

## الشريحة 7 (مُسلَّمة — Phase 12)

آخر ثلاث ميزات تغادر MainViewModel وفق ADR-6 خيار ج — **الحالة الطرفية
المخططة**: القشرة الصادقة فقط.

### ما انتقل ولماذا

- **AgentsViewModel** (كتالوج الوكلاء — درزة activeAgent مع Studio/Tasks):
  الحقلان `availableAgents`/`activeAgent` + المنطق الحرفي الكامل —
  الانطلاقة الباردة المتزامنة من `CanonicalAgentCatalog.defaults` (لا IO
  على الخيط الرئيسي، لا استوديو فارغ — P1-08) يتبعها التحديث السلطوي
  غير المتزامن من السجل الدائم (مع احتياط سجل runtime)، درزة الاختيار
  (الوكيل المختار هو المنفَّذ)، حلقة بانيت الوكلاء P1-10 (إنشاء → صف
  دائم + تسجيل runtime فوري قابل للتنفيذ + بانر)، والحذف الدائم.
  التبعيات: `agentRegistryService` + `componentRegistry`.
- **ExtensionsViewModel** (منظومة الامتدادات): الحقول الأربعة (المهارات/
  الإضافات/خوادم MCP/التكاملات) + جامعاتها الأربعة (حرفياً من
  observeSubsystems) + الدوال السبع (تبديل المهارة/تنفيذها المباشر/
  تبديل الإضافة/تبديل وفحص وتسجيل خادم MCP/ربط التكامل). التبعية:
  `extensionManager` وحدها.
- **ActivityViewModel** (التدفق الموحد للنشاط): `activeExecutionId` +
  `activeExecutionTrace` + `recentAuditEvents` حرفياً — نطاقية GAP-04
  (flatMapLatest على مساحة العمل النشطة)، وربط التتبع بالتنفيذ الحي
  (الإصلاح الأصلي: Started يحمل معرّف التنفيذ الحقيقي — لا استعلام
  هوية فارغة أبداً)، مع **جمع نصيب النشاط من ناقل الاستوديو بنفسه**
  (نمط Governance/Decision — كل ميزة تجمع حصتها). التبعيات:
  `telemetryPort` + `workspaceRuntimeService` + الناقل.

### الحالة الطرفية لـ MainViewModel (610 → 163 سطراً)

ما تبقى هو القشرة الصادقة فقط: بوابة آلة الإقلاع §3A (الطور + رسالة
الفشل + retry)، مرايا العرض النطاقية (activeProject + autonomyPolicy
من WorkspaceRuntimeService)، إسقاطات مساحات العمل للشريط العلوي،
وقناتا الخطأ/البانر العابرتان للقشرة. **حُذفت تبعيتان ميتان كانتا
محفوظتين بلا أي قارئ**: `executeAgentTaskUseCase` و`telemetryService`
(معرّفتان في الباني منذ شرائح سابقة ولا تستخدمان — ازدادت مع كل شريحة
منذ الاستخراج الثاني). UiState فقدت 6 حقول (الكتالوج + المنظومة) وبقي
لها 8 حقول قشرية.

### الشاشات (تركيب owner-VM)

- Studio: يركّب على AgentsViewModel للكتالوج (المنتقي + البطاقة +
  الباني + حذف التأكيد + معامل التنفيذ/الجلسة الجديدة) بينما يبقى على
  MainViewModel لمرآة autonomyPolicy (مالكها — المرآة القشرية).
- Tasks: **اختفت آخر درزة MainViewModel** — الشاشة الآن ثلاثية نظيفة
  (AgentsViewModel لتعيين وكلاء الخطوات + TasksViewModel للوحة +
  WorkflowsViewModel للمنشئ/المكتبة/الاستئناف).
- Extensions: تركّب كاملة على مالكها مع **بانر تشخيصي محلي قابل
  للإخفاء** (نمط الشريحتين 3-4) لنتائج تنفيذ المهارات المباشر.
- Activity: تركّب على مالكها (التدفق + التدقيق).
- Explorer: صف الوكلاء وصف الأدوات يقرآن من المالكين (نمط الصفوف 1-6).
- القشرة: قناتا خطأ جديدتان (Agents/Extensions) في snackbar العام —
  ميزة النشاط تدفقات قراءة فقط (لا قناة خطأ لها).

### ملاحظة صدق موثقة (لا تغيير منتج)

`UiState.isDegraded`/`degradedReason` **بلا كاتب** منذ استخراج الشريحة
الثانية (القراءة الوحيدة: تلوين بانر القشرة `DismissibleInfoBanner`
الذي يقرأه ثابتاً false دائماً) — نفس عائلة GAP-23 (حقل بلا كاتب)،
لكنه خطاف تنسيق كامن لا عدّاد كاذب؛ يوثَّق هنا ويُترك للشريحة 8/قرار
منتج (حذف الحقلين أو كتابتهما من مصدر التدهور الفعلي).

### GAP-21 — الاختبارات السلوكية

- **AgentsViewModelTest** — 8 اختبارات فوق السجل الدائم الحقيقي
  (AgentRegistryService فوق Room في الذاكرة) + ComponentRegistry
  الحقيقي: الانطلاقة الباردة الفورية، التحديث السلطوي يستبدل الكتالوج،
  بقاء الاختيار عبر تحديث يحتوي الوكيل، سقوط الاختيار للأول عند حذفه
  (لا يبقى وكيل نشط يشير لصف محذوف)، إنشاء P1-10 (صف دائم + **تسجيل
  runtime يُتحقق منه بالاستعلام من ComponentRegistry الحقيقي** + اختيار
  فوري + بانر)، الخطأ الصادق بلا سجل، الحذف الدائم (يُتحقق منه بقراءة
  سجل جديدة)، وإخفاء القناتين.
- **ExtensionsViewModelTest** — 7 اختبارات فوق ExtensionManager حقيقي
  (Room في الذاكرة + مهارة وهمية عند PORT فقط): الجامعات الأربعة تعكس
  حقيقة الإقلاع (المهارتان الموثقتان؛ **لا إضافات مفبركة — قائمة فارغة
  صادقة F-11**)، التبديلان ينعكسان عبر الحالة، تنفيذ المهارة المباشر:
  النجاح → بانر الميزة والفشل → قناة خطأ الميزة (رسالة diagnosticMessage
  — عائلة S-2)، تسجيل خادم MCP بحالة UNKNOWN صادقة وبلا أدوات مفبركة،
  والاحتياط الصادق.
- **ActivityViewModelTest** — 6 اختبارات فوق RoomTelemetryRepository
  الحقيقي (Room في الذاكرة) + WorkspaceRuntimeService الحقيقي + ناقل
  محقون: الاحتياط الفارغ الصادق بلا منفذ، نطاقية GAP-04 للنافذتين
  (صفوف المساحة النشطة فقط)، **إعادة النطاق بلا نزيف** (تبديل مساحة لا
  يُظهر صفوف القديمة)، ربط Started بالتنفيذ الحي (يضيق القائمة لعقد
  ذلك التنفيذ فقط)، والهوية الفارغة لا تستعلم أبداً (تبقى نافذة
  المساحة).
- (سباقات جانب اختبار أُصلحت وموثقة بسابقة Providers: **نمط «+1»** —
  بذرة صف إضافي تجعل هبوط جامع init قابلاً للتمييز قبل أي تحوير، وبناء
  VM جديد بعد اكتمال البذر — جامع init للنسخة السابقة يقرأ سجلاً فارغاً
  وقت البناء ولا يكتب بعدها أبداً).

### مصفوفة Roborazzi (امتداد سابقة الشريحة 6)

`Slice7ScreensRoborazziMatrixTest` — لقطتا Pixel8 بـ RTL مثبَّت فوق
خدمات حقيقية: `extensions_loaded` (المهارتان + خادم مسجل UNKNOWN +
مهارة مطفأة عبر الدرزة الحقيقية) و`activity_loaded` (مساحة حقيقية +
عقدا تتبع وحدث تدقيق عبر المنفذ الحقيقي + ربط Started حي) — عبر
recordRoborazziDebug إلى `app/src/test/screenshots/slice7/`.

### التحقق (تشغيلات فعلية، JUnit XML)

- المجموعات الموجهة الثلاث: **21/21 خضراء** (بعد إصلاح سباقات جانب
  الاختبار الموثقة أعلاه).
- كامل `testDebugUnitTest`: **122 suites / 820 tests / 0 failures /
  0 errors / 0 skipped** (ارتفع من 118/797 بـ 21 اختباراً سلوكياً +
  لقطتي Roborazzi).
- `compileDebugKotlin` و`compileDebugUnitTestKotlin` ناجحتان.
- `assembleDebug` ناجحة (100,302,990 بايت) — وتحقق إضافي أن أصناف
  AgentsViewModel وExtensionsViewModel وActivityViewModel داخل dex
  الحزمة (عبر ملفات dex الـ21).

## الشريحة 8 (مُسلَّمة — Phase 13: قرار i18n + التنظيف القشري النهائي + إصلاح فشل CI للشريحة 7)

### إصلاح فشل CI أولاً (سبب جذر مُعاد إنتاجه وتشخيصه، لا تخمين)

الشريحة 7 فشلت على GitHub CI بينما كانت خضراء محلياً. أُعيد إنتاج
الفشل **بنفس أعلام CI** (`--no-daemon --rerun-tasks`):

- **`WorkflowsViewModelTest::save then list then load`** — سباق جانب
  اختبار من عائلة «انتظر الإشارة الطرفية المحددة» الموثقة: انتظار
  `diagnosticBanner != null` بعد إعادة الحفظ كان يمر **فارغاً** على
  بانر الحفظ الأول غير المصروف، فلا يثبت هبوط الكتابة الثانية، ثم
  يقرأ التحميل صفاً قبل الحفظ فلا يرى الهدف المعدّل أبداً. الإصلاح:
  صرف البانر أولاً (`dismissDiagnosticBanner`) قبل إعادة الحفظ.
- **`Slice7ScreensRoborazziMatrixTest::activity_screen_loaded_feed`** —
  استثناء غير ملتقط `connection pool has been closed`: اختبار المصفوفة
  يغلق قاعدة Room في `finally` بينما جامعات `ActivityViewModel` القائمة
  (تدفق `forExecution` المُعاد اشتراكه بعد إشارة Started) ما زلن في
  `viewModelScope` — خطوة استعلام معلقة تحت مجمّع مُغلق. الإصلاح
  الجذري: **إلغاء نطاقات VM والخدمات قبل إغلاق القاعدة** (مكافئ
  `onCleared()` جانب الاختبار).
- **تحصين العائلة كلها** (بمسح كل ملفات اختبار VM التي تغلق Room وهي
  تحمل جامعات قائمة): `ActivityViewModelTest` و`ProvidersViewModelTest`
  (إلغاء في `@After`)، `WorkflowsViewModelTest` (+ إلغاء `lateViewModel`
  المحلي)، ومصفوفة الشريحة 6 (اختبار Tasks). الاستثناء الموثق بعد
  التدقيق: `AgentsViewModel`/`ExtensionsViewModel`/`RadarViewModel`/
  `GovernanceViewModel` بلا جامعات Room قائمة (قراءات لمرة واحدة أو
  StateFlows في الذاكرة) — لم تُلمس.
- **`e2e-device.yml` كان يفشل في خطوة «Set up Android SDK» نفسها منذ
  الشريحة 5** (action `android-actions/setup-android@v3` مكسور على صورة
  ubuntu-latest الحالية — الاختبارات الآلية لم تُشغَّل قط: skipped).
  أُزيل الـ action واستُبدل بقبول تراخيص SDK المثبت مسبقاً على
  الصورة مباشرة.
- **`bundleRelease`** (بوابة جهوزية الإصدار في android.yml — لم تكن
  تُتحقق محلياً قط لأن CI كان يتوقف قبلها): نجحت محلياً مع R8 كاملاً
  (AAB غير موقعة 36,561,616 بايت).

### قرار i18n — D-12 (موثق بالكامل في PRODUCT-DECISIONS.md)

العربي-فقط مع السلاسل المضمّنة يبقى **الحالة النهائية المتعمدة**
للمسار: لم تُنشأ الشريحات أي شاشات جديدة (كل شريحة أعادت هيكلة شاشة
قائمة فوق VM ميزتها) فلم يُفعَّل بند D-1 «الشاشات الجديدة تعتمد
الموارد من اليوم الأول» قط. القاعدة القائمة: أي توسع لغوي يستلزم
استخراج السلاسل شاشة-بشاشة كمتطلب مسبق — والمسار جعله قابلاً للتنفيذ:
لكل شاشة الآن VM مالك واحد تثبّت مجموعته السلوكية (GAP-21) سلوكها.

### التنظيف القشري النهائي — D-13 (موثق بالكامل في PRODUCT-DECISIONS.md)

الحقول الثلاثة الميتة حُذفت لا وُظِّفت: `UiState.isDegraded`/
`degradedReason` (بلا كاتب منذ الشريحة 2) و`diagnosticBanner` القشرية
(بلا كاتب أصلاً — كتلة البانر في MainAppScreen لم تكن قادرة على عرض
أي شيء قط، ودالة إخفائها كانت تخفي قناة لا يكتبها أحد). لا يوجد مصدر
تدهور صادق على مستوى القشرة: بوابة الإقلاع لها سطح فشلها الصريح
المستقل، وعرض التدهور الحقيقي يعيش عند مالكيه (بانر Studio التنفيذي،
سطر DEGRADED في تقرير خطط العمل، صفوف التنفيذ الدائمة). مع الحذف:
`dismissDiagnosticBanner()` وكتلة البانر القشرية واستيراد
`DegradedReason` من UiState.

### GAP-21 — اختبار عقد القشرة الختامي

`MainViewModelTest` — 7 اختبارات فوق الحزمة الحقيقية كاملة (سابقة
BootstrapStateMachineTest: Room حقيقي في الذاكرة + منسّق الإقلاع
الحقيقي + WorkspaceRuntimeService حقيقية، بتوصيل AppContainer نفسه):
مرآة الإقلاع الحقيقي حتى READY بلا رسالة فشل، مرآة الفشل الصادقة
(الطور + الرسالة العربية لبوابة الشاشة الكاملة)، إغلاق البوابة عبر
التدفق نفسه عند التعافي، عدم-تأثيرية `retryBootstrap` على المنسّق
الحقيقي، مرآة مساحة العمل الافتراضية مع مشروعها الرملي المملوك
(P0-04: معرّف حقيقي > 0 لا «مشروع 1» صامت)، مرآة الاستقلالية تتبع
العمود المخزّن عبر إعادة نطاق كاملة، وقناة الخطأ الصادقة (انحدار
R-5: فشل جامع في مسار init يظهر بدل أن يُسقط التطبيق) + إخفاؤها.

### التحقق (تشغيلات فعلية، JUnit XML مُفسَّر)

- الموجهة (5 أجنحة المُصلَحة + الجديدة): **48/48 خضراء**.
- كامل `testDebugUnitTest` بشروط CI نفسها (`--no-daemon
  --rerun-tasks`): **123 suites / 827 tests / 0 failures / 0 errors /
  0 skipped** (ارتفاع من 122/820 بـ 7 اختبارات القشرة).
- `compileDebugKotlin` + `compileDebugUnitTestKotlin` +
  `compileDebugAndroidTestKotlin` ناجحة.
- `assembleDebug` ناجحة (100,302,990 بايت) و`bundleRelease` ناجحة
  (AAB غير موقعة 36,563,932 بايت — R8 + lintVital).

## خاتمة المسار — 8/8 شرائح مُسلَّمة

المسار مكتمل: من MainViewModel واحدة بـ2293 سطراً و~50 تبعية إلى قشرة
صادقة بـ165 سطراً (بوابة إقلاع + مرايا عرض + قناة خطأ واحدة) مع 11
ViewModel ميزة، كلٌّ منها مالك مصدر حقيقته الواحد ومثبّت بمجموعته
السلوكية. لا شرائح تالية على هذا المسار.

## قواعد المسار (موروثة من الخطة)

- أي ميزة جديدة → ViewModel ميزة جديد فقط (تجميد توسيع MainViewModel).
- كل VM ميزة = اختبارات سلوكية (GAP-21) قبل اعتبار الشريحة مكتملة.
- لا إعادة تصميم لشاشة قبل استخراج VM الخاصة بها (خيار ج).
- الصدق الظاهري أولاً: أي نص/عدّاد في UI يجب أن يقرأ مصدر حقيقة واحد.
